server:
  port: 8080
  servlet:
    encoding:
      charset: utf-8
      force: true

spring:
  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        '[format_sql]': true 